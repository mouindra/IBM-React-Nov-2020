import cartLoad from "./load";
import addToCart from './addToCart';
import removeFromCart from './removeFromCart';

const actionCreators = { 
    cartLoad, addToCart, removeFromCart
};

export default actionCreators;